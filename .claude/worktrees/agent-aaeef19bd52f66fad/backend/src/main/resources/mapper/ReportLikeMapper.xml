<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.reportapp.reportappbackend.mapper.ReportLikeMapper">

    <select id="findByDailyReportIdAndLiker" resultType="com.reportapp.reportappbackend.entity.ReportLike">
        SELECT id, daily_report_id, liker_type, liker_id, created_at, updated_at
        FROM report_likes
        WHERE daily_report_id = #{dailyReportId} AND liker_type = #{likerType} AND liker_id = #{likerId}
    </select>

    <select id="countByDailyReportId" resultType="int">
        SELECT COUNT(*)
        FROM report_likes
        WHERE daily_report_id = #{dailyReportId}
    </select>

    <insert id="insert">
        INSERT INTO report_likes (id, daily_report_id, liker_type, liker_id)
        VALUES (#{id}, #{dailyReportId}, #{likerType}, #{likerId})
    </insert>

    <delete id="deleteByDailyReportIdAndLiker">
        DELETE FROM report_likes
        WHERE daily_report_id = #{dailyReportId} AND liker_type = #{likerType} AND liker_id = #{likerId}
    </delete>

</mapper>
