<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.reportapp.reportappbackend.mapper.ReportImageMapper">

    <select id="findById" resultType="com.reportapp.reportappbackend.entity.ReportImage">
        SELECT id, daily_report_id, image_url, created_at, updated_at
        FROM report_images
        WHERE id = #{id}
    </select>

    <select id="findByImageUrl" resultType="com.reportapp.reportappbackend.entity.ReportImage">
        SELECT id, daily_report_id, image_url, created_at, updated_at
        FROM report_images
        WHERE image_url = #{imageUrl}
    </select>

    <select id="findByDailyReportId" resultType="com.reportapp.reportappbackend.entity.ReportImage">
        SELECT id, daily_report_id, image_url, created_at, updated_at
        FROM report_images
        WHERE daily_report_id = #{dailyReportId}
        ORDER BY created_at ASC
    </select>

    <insert id="insert">
        INSERT INTO report_images (id, daily_report_id, image_url)
        VALUES (#{id}, #{dailyReportId}, #{imageUrl})
    </insert>

    <delete id="deleteById">
        DELETE FROM report_images
        WHERE id = #{id}
    </delete>

</mapper>
