<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.reportapp.reportappbackend.mapper.ReportItemResponseMapper">

    <select id="findByDailyReportId" resultType="com.reportapp.reportappbackend.entity.ReportItemResponse">
        SELECT id, daily_report_id, report_item_definition_id, checked, text_value, created_at, updated_at
        FROM report_item_responses
        WHERE daily_report_id = #{dailyReportId}
    </select>

    <delete id="deleteByDailyReportId">
        DELETE FROM report_item_responses
        WHERE daily_report_id = #{dailyReportId}
    </delete>

    <insert id="insertBatch">
        INSERT INTO report_item_responses (id, daily_report_id, report_item_definition_id, checked, text_value)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.id}, #{item.dailyReportId}, #{item.reportItemDefinitionId}, #{item.checked}, #{item.textValue})
        </foreach>
    </insert>

</mapper>
